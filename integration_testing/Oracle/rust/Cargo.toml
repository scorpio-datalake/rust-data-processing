[package]
name = "rdp-oracle-integration-test"
version = "0.1.0"
edition = "2021"
publish = false
description = "Opt-in Oracle integration tests (self-contained under integration_testing/Oracle/)."

[lib]
path = "src/lib.rs"

[dependencies]
oracle = "0.6"
rust-data-processing = { path = "../../../", features = ["db_connectorx"] }

[dev-dependencies]
serde_json = "1"
