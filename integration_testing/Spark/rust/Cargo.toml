[package]
name = "rdp-spark-integration-test"
version = "0.1.0"
edition = "2021"
publish = false
description = "Opt-in Spark file:// stage integration tests."

[lib]
path = "src/lib.rs"

[dependencies]
libloading = "0.8"
serde_json = "1"
rust-data-processing = { path = "../../../", features = ["integration_full"] }
