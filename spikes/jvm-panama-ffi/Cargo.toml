[package]
name = "rdp-jvm-ffi-spike"
version = "0.1.0"
edition = "2021"
publish = false
description = "Phase 3 spike: minimal cdylib C ABI for Project Panama / jextract + optional JDK 21 FFM smoke (not published to crates.io)."
license = "MIT OR Apache-2.0"
rust-version = "1.71"

[lib]
crate-type = ["cdylib", "rlib"]
name = "rdp_jvm_ffi_spike"

[dependencies]
