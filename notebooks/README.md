# Notebooks

| Notebook | Description |
|----------|-------------|
| [`ml_qa.ipynb`](ml_qa.ipynb) | Tabular **ML QA**: ingest → profile → validate → optional transform (requires `pip install rust-data-processing` + fixture paths). |
| [`quickstart.ipynb`](quickstart.ipynb) | Minimal import + `DataSet` smoke test. |

## Install

```bash
pip install rust-data-processing
```

Use **absolute paths** to CSV fixtures in this repo (e.g. `tests/fixtures/people.csv`) when running locally.
